kind = "web"
previewPath = "/"
title = "Auckland WebGIS"
version = "1.0.0"
id = "artifacts/webgis"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26061

[services.development]
run = "pnpm --filter @workspace/webgis run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/webgis", "run", "build" ]
publicDir = "artifacts/webgis/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26061"
BASE_PATH = "/"
